<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yijun.finaltruefinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".truefinal">    `
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        //new activity called chemistry//
        <activity android:name=".chemistry">

        </activity>
        //new activity called math//
        <activity android:name=".math">

        </activity>
        //new activity called phy//
        <activity android:name=".phy">

        </activity>
        //new activity called his//
        <activity android:name=".his">

        </activity>
        //new activity called instructions
        <activity android:name=".instructions">

        </activity>
    </application>
     <uses-permission android:name="android.permission.VIBRATE" />

</manifest>